R General regression neural network realisation by Liora Pospelova (posvika@gmail.com)
with multidimentional input and scalar output

05/05/15 hadashot:
	* про веса:
	1 нейрон скрытого слоя соответсвует одного примеру набора training, входные веса от входного слоя от нейрона j к скрытому слою равны значениям в столбцах j training set
	выходные веса к единственному нейрону выходного слоя равны значения в target столбце

	* добавить нормировку данных, сохранить множители, чтобы потом вернуться назад
	найти абсолютный (по модулю) минимум, абсолютный максимум
	нормированная ячейка = (ячейка - абс.минимум)/(абс.макс-абс.мин)
	или
	нормированная ячейка = (ячейка)/(абс.макс) #без сжимания к нулю
	или 
	нормированная ячейка = (ячейка - обычный.минимум)/(обычный.макс-обычный.мин) # с потерей знака
	
